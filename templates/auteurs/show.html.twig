{% extends 'base.html.twig' %}

{% block title %}Auteurs{% endblock %}

{% block body %}
    <h1>Auteurs</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ auteur.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ auteur.name }}</td>
            </tr>
            <tr>
                <th>Date_at</th>
                <td>{{ auteur.dateAt ? auteur.dateAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Relation</th>
                <td>{{ auteur.relation }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_auteurs_index') }}">back to list</a>

    <a href="{{ path('app_auteurs_edit', {'id': auteur.id}) }}">edit</a>

    {{ include('auteurs/_delete_form.html.twig') }}
{% endblock %}
